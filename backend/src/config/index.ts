export const config = {
  // TODO: UPDATE THIS URL TO THE PRODUCTION URL
  appName: "GymSimple",
  site: process.env.NODE_ENV === "development" ? "http://localhost:3000" : "https://gymsimple.vercel.app",
  email: "danielcracbusiness@gmail.com",
};